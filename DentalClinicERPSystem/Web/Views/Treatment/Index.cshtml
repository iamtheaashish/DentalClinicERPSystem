@model IEnumerable<Business.DTO.TreatmentDto>

<div class="container mt-3">
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-3">
        <div>
            <h3 class="fw-bold mb-1">Treatment List</h3>
        </div>
        <div>
            <a asp-controller="Treatment" asp-action="Create" class="btn btn-primary shadow-sm">
                <i class="bi bi-plus-lg me-1"></i> New Treatment
            </a>
        </div>
    </div>

    <div class="table-responsive">
        <table class="table table-striped table-hover table-bordered align-middle">
            <thead class="table-dark">
                <tr>
                    <th>Treatment Id</th>
                    <th>Patient Id</th>
                    <th>Appointment Id</th>
                    <th>Treatment Date</th>
                    <th class="text-center">Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (!Model.Any())
                {
                    <tr>
                        <td colspan="8" class="text-center text-muted">No treatment records found.</td>
                    </tr>
                }
                else
                {
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Id</td>
                            <td>@item.PatientId</td>
                            <td>@item.AppointmentId</td>
                            <td>@item.TreatmentDate.ToString("dd MMM yyyy")</td>
                            <td class="text-center">
                                <div class="btn-group" role="group">
                                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info">
                                        Details
                                    </a>
                                    <a asp-action="Update" asp-route-id="@item.Id" class="btn btn-sm btn-outline-warning">
                                        Edit
                                    </a>
                                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger">
                                        Delete
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>